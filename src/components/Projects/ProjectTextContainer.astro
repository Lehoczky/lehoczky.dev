---
import type { HTMLAttributes } from "astro/types"

import { cn } from "@/libs/cn"

type Props = HTMLAttributes<"div">

const { class: className, ...attrs } = Astro.props
---

<div
  class={cn("grid xl:max-w-xl xl:justify-self-center", className)}
  {...attrs}
>
  <slot />
</div>
